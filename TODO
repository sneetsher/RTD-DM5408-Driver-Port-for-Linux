ToDo:

Module:

- IOCTL handler..done
- Autotools for building
- DKMS, packaging
- Registers masks
- IRQ Handlers
- DMA Handles

Library:

- Add a function could assign io base address on runtime.

Demo:

- Read digital ports..done
